package scanner;

//Enum representing different types of tokens produced by the scanner
public enum TokenType {
  IDENTIFIER,
  INTEGER,
  STRING,
  OPERATOR,
  DELETE,
  L_PAREN,
  R_PAREN,
  SEMICOLON,
  COMMA,
  RESERVED;
}
